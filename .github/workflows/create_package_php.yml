name: Create package Node
 
on:
  workflow_dispatch:
  pull_request:
    types:
      - closed

    branches:
      - 'main'

jobs:
  if_merged:
    name: Create artifacts and push to package branch
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout main
        uses: actions/checkout@v3
        with:
          ref: main

      - name: PHP files changed check
        id: php-files
        uses: tj-actions/changed-files@v35
        with:
          files: php/**

      - name: List all PHP changed files
        run: |
          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            echo "$file was changed"
          done

      - name: Upload PHP artifact
        if: steps.php-files.outputs.any_changed == 'true'
        uses: actions/upload-artifact@v3
        with:
          name: php-artifact
          path: php

      - name: Checkout packages
        if: steps.node-files.outputs.any_changed == 'true'
        uses: actions/checkout@v3
        with:
          ref: packages

      - name: Download php artifact
        if: steps.php-files.outputs.any_changed == 'true'
        uses: actions/download-artifact@v3
        with:
          name: php-artifact
          path: php

      - name: Archive PHP
        if: steps.php-files.outputs.any_changed == 'true'
        uses: thedoctor0/zip-release@0.7.0
        with:
          path: php
          type: 'zip'
          filename: 'php.zip'

      - name: Push PHP to packages branch
        if: steps.php-files.outputs.any_changed == 'true'
        run: |
          git config --local user.name armandojaleo
          git config --local user.email armando@armandojaleo.com
          git checkout packages
          git add php.zip
          git commit -m "Add new PHP package"
          git push

  